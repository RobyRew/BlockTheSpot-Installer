{
  "name": "blockthespot-catalog",
  "private": true,
  "type": "module",
  "version": "0.4.0",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "test": "node --test ../scripts/test-site.mjs",
    "catalog:update": "node scripts/update-catalog.mjs"
  },
  "dependencies": { "astro": "7.3.3" }
}
